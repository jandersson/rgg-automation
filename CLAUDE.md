# Repo instructions

Build/test/run commands, conventions (work on `main`, **Conventional Commits**,
don't push unless asked, keep tests green), and gotchas live in `AGENTS.md` —
imported below so Claude Code and any other agent share one source of truth.

@AGENTS.md
